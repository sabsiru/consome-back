version: '3.8'
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: consome
      MYSQL_USER: consome
      MYSQL_PASSWORD: consome
    ports:
      - "3306:3306"