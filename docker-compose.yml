version: '3.8'
services:
  mysql:
    image: mysql:8.0
    container_name: consome-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: consome
      MYSQL_USER: consome
      MYSQL_PASSWORD: consome
    ports:
      - "13306:3306"