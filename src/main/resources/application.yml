spring:
  datasource:
    url: jdbc:mysql://localhost:3306/consome?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: consome
    password: consome123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

logging:
  level:
    org.hibernate.SQL: debug