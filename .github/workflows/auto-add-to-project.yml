name: Auto-add issues to GitHub Project

on:
  issues:
    types: [opened]

jobs:
  add-to-project:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/add-to-project@v1
        with:
          project-id: PVT_kwHOBwtDt84A5GmN
          github-token: ${{ secrets.GITHUB_TOKEN }}